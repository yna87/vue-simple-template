# GitHub repository information
# Update these values for your project
GITHUB_ORG=your-github-org
GITHUB_REPO=your-repo-name

# Optional: Specify stack name (defaults to: {GITHUB_REPO}Stack)
# STACK_NAME=my-custom-stack-name

# Optional: Reuse existing GitHub OIDC Provider ARN
# If you already have a GitHub OIDC Provider in your AWS account, specify its ARN here
# GITHUB_OIDC_PROVIDER_ARN=arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com

# Optional: AWS region (defaults to: ap-northeast-1)
# CDK_DEFAULT_REGION=ap-northeast-1

# Optional: Enable Basic Authentication
# Uncomment and set these values to enable Basic Auth on CloudFront
# BASIC_AUTH_USER=admin
# BASIC_AUTH_PASS=your-secure-password
